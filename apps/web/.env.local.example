NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Server-only: must match apps/api's API_KEY. Never prefix with NEXT_PUBLIC_ —
# this must stay out of the browser bundle. Used only by Server Actions.
API_KEY=
