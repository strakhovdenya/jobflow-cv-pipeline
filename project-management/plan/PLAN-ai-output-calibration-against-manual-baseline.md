# Plan: AI Output Calibration Against Manual Baseline (EPIC-24 / Phase 17)

**PRD:** `project-management/prd/PRD-ai-output-calibration-against-manual-baseline.md`
**Дата:** 2026-08-19

**Решения по открытым вопросам PRD (зафиксированы с project owner перед этим планом):**
- Golden dataset физически хранится в `project-management/golden-dataset/` (новая папка,
  отдельно от `apps/api` runtime storage и от `knowledge-sources/`).
- Посекционное сравнение AI-результата с ручным baseline и фиксацию вердикта (match/partial/
  mismatch) выполняет Claude Code, не project owner вручную — поэтому фаза 4 ниже имеет чёткие,
  проверяемые Acceptance Criteria, а не остаётся ручным шагом вне Claude Code.
- Остальные открытые вопросы PRD (сколько golden-кейсов минимально нужно; объём аудита web-app
  допущений; нужно ли поле `needs_verification`) явно оставлены "решается по факту" — PRD сам
  говорит, что они определяются только после реального прочтения `!prompt_1.../!prompt_2...`
  текста и реальных папок с завершёнными заявками, не заранее.

## Фазы реализации

### Фаза 1: Импорт и адаптация Prompt 1 текста
**Цель:** `PromptTemplate` для `prompt_1` основан на реальном `!prompt_1_0_3_...txt`, а не на
placeholder-контенте; все web-app-специфичные допущения этого шага явно разобраны.
**Затрагивает:** apps/api (PromptTemplate content, `pipeline/schemas/vacancy-analysis.schema.ts`
если аудит потребует новое поле), `apps/api/prisma/seed.ts`
**Задачи:**
- [ ] Прочитать `!prompt_1_0_3_...txt` целиком (PRD прочитал только фрагмент) и составить список
      всех web-app-специфичных допущений (browsing, вложения, session memory и что найдётся сверх
      этих трёх)
- [ ] По каждому найденному допущению: либо замаппить на существующий механизм (carry-forward,
      manual note, JSON-schema output), либо переформулировать инструкцию с явным fallback
      (`needs_verification`-подобным) — ни одно не отбрасывать молча
- [ ] Адаптировать текст в новую версию `PromptTemplate` (`prompt_1`), сохранив старую версию
      неактивной (не перезаписывать)
- [ ] Верифицировать Anti-Overclaiming Rules против адаптированного текста (needs evidence,
      commercial/personal разделение) — не считать унаследованным автоматически из ручного флоу
- [ ] Убрать/обновить в `apps/api/prisma/seed.ts` комментарий "Placeholder content pending
      prompt-engineering review" для `prompt_1`

**Когда готова:** закрывает AC "`PromptTemplate` контент для `prompt_1`/`prompt_2` основан на
реальном `!prompt_1_0_3_...txt`/`!prompt_2_0_1_...txt` тексте... `prisma/seed.ts` больше не
помечает эти шаги как 'Placeholder content pending prompt-engineering review'" (для `prompt_1`
части), AC "Каждое найденное web-app-специфичное допущение... явно задокументировано с решением"
(для `prompt_1`) и AC "Anti-overclaiming правила... верифицированы против адаптированного текста
промптов" (для `prompt_1`).

### Фаза 2: Импорт и адаптация Prompt 2 текста
**Цель:** `PromptTemplate` для `prompt_2` основан на реальном `!prompt_2_0_1_...txt`, используя
тот же согласованный в Фазе 1 подход к аудиту допущений и к решению по `needs_verification`.
**Затрагивает:** apps/api (PromptTemplate content, `pipeline/schemas/targeted-cv-content.schema.ts`
если требуется), `apps/api/prisma/seed.ts`
**Задачи:**
- [ ] Прочитать `!prompt_2_0_1_...txt` целиком и составить список web-app-специфичных допущений
- [ ] По каждому допущению: замаппить на существующий механизм или переформулировать с fallback —
      используя тот же подход (и то же решение по `needs_verification`), что принят в Фазе 1
- [ ] Адаптировать текст в новую версию `PromptTemplate` (`prompt_2`), старая версия остаётся
      неактивной
- [ ] Верифицировать Anti-Overclaiming Rules против адаптированного текста `prompt_2`
- [ ] Убрать/обновить в `apps/api/prisma/seed.ts` комментарий "Placeholder content pending
      prompt-engineering review" для `prompt_2`

**Когда готова:** закрывает AC "`PromptTemplate` контент для `prompt_1`/`prompt_2` основан на
реальном тексте... `prisma/seed.ts` больше не помечает эти шаги как placeholder" (для `prompt_2`
части), AC "Каждое найденное web-app-специфичное допущение... явно задокументировано" (для
`prompt_2`) и AC "Anti-overclaiming правила... верифицированы" (для `prompt_2`).

### Фаза 3: Golden dataset — сбор и документирование
**Цель:** Реальные завершённые папки (`Action1/`, `Amach/` и др.) отобраны и записаны как golden
dataset в согласованном формате и месте хранения.
**Затрагивает:** project-management (`project-management/golden-dataset/` — новая папка)
**Задачи:**
- [ ] Просмотреть реально обработанные папки (см. `docs/00_product_vision_updated_consistent.md`
      §3) и отобрать те, где есть и исходный текст вакансии, и реально отправленное CV (и skip
      reason, если применимо)
- [ ] Создать `project-management/golden-dataset/` и задокументировать каждый отобранный кейс в
      формате `docs/10_calibration_and_parity.md` §3.2
- [ ] Убедиться, что набор содержит ≥1 реальный кейс на каждый исход (apply, maybe, skip) — если
      такие кейсы реально существуют в истории; если какого-то исхода нет в истории, явно это
      отметить, а не оставить пробел незамеченным

**Когда готова:** закрывает AC "Golden dataset существует, задокументирован в формате
`docs/10_calibration_and_parity.md` §3.2, и содержит ≥1 реальный кейс на каждый исход (apply,
maybe, skip — если такие кейсы реально существуют в истории project owner)".

### Фаза 4: Golden dataset — decision-level прогон и сравнение
**Цель:** Каждый golden-кейс прогнан через Prompt 1 через `apps/web` UI, и для каждого
зафиксирован decision-level (apply/maybe/skip) вердикт сравнения с ручным baseline.
**Затрагивает:** apps/api (реальные Prompt 1 прогоны через `apps/web` UI, без новых endpoint'ов),
project-management (лог сравнений)
**Задачи:**
- [x] Прогнать каждый golden-кейс из Фазы 3 через реальный pipeline (Prompt 1 → Prompt 2, где
      применимо) через `apps/web` UI, с реальным knowledge-source контентом и manual note (#206)
- [x] Для каждого кейса зафиксировать decision match (apply/maybe/skip, да/нет + note) (#207)

**Когда готова:** закрывает decision-level часть AC "Для каждого golden-кейса зафиксирован
результат сравнения: decision match (да/нет + note)...". Content-level часть того же AC
переехала в Фазу 6 (см. ниже).

### Фаза 5: Итерация до convergence — Prompt 1 (decision-level)
**Цель:** По результатам Фазы 4 создаются новые версии `PromptTemplate` для `prompt_1` (при
необходимости) и повторяются прогоны, пока decision-level результат по всему golden-набору не
удовлетворит convergence criteria или расхождения не будут задокументированы как принятые
исключения.
**Затрагивает:** apps/api (новые версии `PromptTemplate` для `prompt_1`), project-management (лог
итераций/исключений)
**Задачи:**
- [ ] По каждому decision-level mismatch/partial из Фазы 4 определить причину и внести правку в
      новую версию `PromptTemplate` для `prompt_1` (не перезаписывая предыдущую)
- [ ] Повторно прогнать затронутые golden-кейсы (Prompt 1 only) и обновить лог сравнений
- [ ] Повторять цикл правка → прогон → сравнение, пока не выполнены convergence criteria из
      `docs/10_calibration_and_parity.md` §5, либо документировать принятое исключение с
      обоснованием для расхождений, которые решено не устранять
- [ ] Убедиться, что версионная история `PromptTemplate` для `prompt_1` отражает все итерации
      калибровки (несколько версий, предыдущие неактивны, не перезаписаны)

**Когда готова:** закрывает decision-level часть AC "Convergence criteria... выполнены..." и AC
"Версионная история `PromptTemplate`..." (для `prompt_1`). Разблокирует Фазу 6.

**Оформление раундов на GitHub (решено 2026-08-19):** отдельный issue на каждый раунд цикла
"правка → перепрогон → сравнение" (не один issue на весь процесс). Issue-трекер `#212` держит
чек-лист раундов и не закрывается до decision-level конвергенции; каждый раунд — новый issue по
образцу `#214` ("Раунд 1 калибровки"), заводится только если предыдущий раунд не привёл к
конвергенции.

### Фаза 6: Golden dataset — content-level прогон и сравнение (Prompt 2)
**Цель:** Для каждого golden-кейса, где `02_targeted_cv_content` существует, выполнено
посекционное сравнение CV-контента с ручным baseline.
**Затрагивает:** apps/api (реальные Prompt 2 прогоны через `apps/web` UI), project-management
(лог сравнений)

**Решение (2026-08-23, зафиксировано при обсуждении #207/#208):** эта фаза **не стартует, пока
issue-трекер Фазы 5 (`#212`) не закрыт**. Причина: `Prompt2InputBuilderService.readAnalysisArtifact()`
читает `01_vacancy_analysis.json` (вывод Prompt 1) как контекст для генерации CV — сравнивать
контент, сгенерированный поверх ещё не откалиброванного анализа, даёт нестабильный сигнал, который
придётся полностью пересчитывать после каждой правки `prompt_1` в Фазе 5. Применимо только к
кейсам, где Prompt 2 реально запускался (оба решения — человека и AI — в зоне apply/maybe); кейсы,
где хотя бы одна сторона решила skip, исключены — CV-контента для сравнения нет.

**Задачи:**
- [ ] Для каждого applicable кейса выполнить посекционное сравнение CV-контента (headline,
      summary, top skills, experience bullets, evidence table/`needs evidence` флаги) по
      методологии `docs/10_calibration_and_parity.md` §4 и зафиксировать вердикт match/partial/
      mismatch + note (#208)
- [ ] Записать результаты (#207 + #208) в текстовом/табличном виде, пригодном для последующего
      аудита (не устный вывод в чате) (#209)

**Когда готова:** закрывает content-level часть AC "Для каждого golden-кейса зафиксирован
результат сравнения: ... посекционный вердикт для CV-контента (match/partial/mismatch + note)".

### Фаза 7: Итерация до convergence — Prompt 2 (content-level)
**Цель:** По результатам Фазы 6 создаются новые версии `PromptTemplate` для `prompt_2` (при
необходимости) и повторяются прогоны, пока content-level результат по applicable golden-набору не
удовлетворит convergence criteria или расхождения не будут задокументированы как принятые
исключения.
**Затрагивает:** apps/api (новые версии `PromptTemplate` для `prompt_2`), project-management (лог
итераций/исключений)
**Задачи:**
- [ ] По каждому content-level mismatch/partial из Фазы 6 определить причину и внести правку в
      новую версию `PromptTemplate` для `prompt_2` (не перезаписывая предыдущую)
- [ ] Повторно прогнать затронутые golden-кейсы (Prompt 1 → Prompt 2) и обновить лог сравнений
- [ ] Повторять цикл правка → прогон → сравнение, пока не выполнены convergence criteria из
      `docs/10_calibration_and_parity.md` §5, либо документировать принятое исключение с
      обоснованием для расхождений, которые решено не устранять
- [ ] Убедиться, что версионная история `PromptTemplate` для `prompt_2` отражает все итерации
      калибровки (несколько версий, предыдущие неактивны, не перезаписаны)

**Когда готова:** закрывает content-level часть AC "Convergence criteria... выполнены..." и AC
"Версионная история `PromptTemplate`..." (для `prompt_2`).

**Оформление раундов на GitHub:** тот же паттерн, что в Фазе 5 — issue-трекер `#237` держит
чек-лист раундов, каждый раунд — новый issue по образцу `#238` ("Раунд 1 калибровки (Фаза 7)"),
заводится только если предыдущий раунд не привёл к конвергенции.

---

Рекомендуется epic base branch: `task/ISSUE-193-ai-calibration-base` (ADR-025/ADR-030 — `193` —
самый нижний номер issue этого эпика, issue #193 "Прочитать `!prompt_1_0_3_...txt`..."), т.к. это
семифазный эпик с независимо тестируемыми частями (импорт Prompt 1, импорт Prompt 2, сбор
датасета, decision-level прогон и сравнение, decision-level конвергенция, content-level прогон и
сравнение, content-level конвергенция), которые не имеет смысла закрывать одним PR, и `main`
должен оставаться releasable на всём протяжении калибровочных итераций (может потребовать
несколько раундов Фаз 5 и 7).

**Порядок Фаз 4–7 (решено 2026-08-23):** Фазы 4 и 5 (decision-level, Prompt 1) выполняются перед
Фазами 6 и 7 (content-level, Prompt 2), а не параллельно — Prompt 2 читает вывод Prompt 1 как
контекст, так что калибровать его контент до конвергенции Prompt 1 бессмысленно: результат
придётся полностью пересчитывать после каждой правки `prompt_1`. См. решение в Фазе 6 выше.
