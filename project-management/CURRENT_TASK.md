# Current Task

## No active task

Last completed: TASK-071 (Add existing-folder import UI) — DONE, branch
`task/TASK-071-existing-folder-import-ui`. See `project-management/TASK_BOARD.md` "Current Focus" for the
full summary and recommended next task (TASK-072).

Per Operating Rules (`CLAUDE.md`), Claude Code must not select the next task automatically — wait
for explicit user selection.
