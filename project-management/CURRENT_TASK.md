# Current Task

## No active task

Last completed: TASK-062 (Add unit/component test runner and coverage to apps/web) — DONE, branch
`task/TASK-062-web-test-runner`. See `project-management/TASK_BOARD.md` "Current Focus" for the
full summary and recommended next task.

Per Operating Rules (`CLAUDE.md`), Claude Code must not select the next task automatically — wait
for explicit user selection.
