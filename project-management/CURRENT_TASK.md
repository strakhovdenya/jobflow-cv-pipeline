# Current Task

## No active task

Last completed: TASK-059 (Add integration tests for database persistence assumptions) — DONE,
branch `task/TASK-059-postgres-persistence-check`. See `project-management/TASK_BOARD.md` "Current
Focus" for the full summary and recommended next task.

Per Operating Rules (`CLAUDE.md`), Claude Code must not select the next task automatically — wait
for explicit user selection.
